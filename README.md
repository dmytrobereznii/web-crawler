# web-crawler

A distributed web crawler built in Go. Submit a URL via HTTP API, watch it get crawled concurrently across a worker pool, follow extracted links up to a configurable depth, and retrieve structured results.
